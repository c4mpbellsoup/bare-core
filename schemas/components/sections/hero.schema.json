{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"$id": "https://raw.githubusercontent.com/c4mpbellsoup/bare-schema/refs/heads/master/schemas/components/sections/hero.schema.json",
	"title": "Hero",
	"type": "object",

	"properties": { "type": { "const": "hero" } },
	"required": ["type"],

	"oneOf": [
		{ "$ref": "#/$defs/basicHero" },
		{ "$ref": "#/$defs/carouselHero" }
	],

	"$defs": {
		"action": {
			"title": "Action",
			"type": "object",
			"required": ["label", "href"],
			"properties": {
				"label": { "$ref": "../../shared/label.schema.json" },
				"href": { "$ref": "../../shared/href.schema.json" }
			}
		},

		"heroBase": {
			"title": "Hero Base",
			"type": "object",
			"required": ["image", "heading"],
			"properties": {
				"image": { "type": "string" },
				"heading": { "type": "string" },
				"subheading": { "type": "string" },
				"actions": { "type": "array", "items": { "$ref": "#/$defs/action" } }
			}
		},

		"basicHero": {
			"title": "Basic Hero",
			"type": "object",
			"allOf": [
				{ "$ref": "#/$defs/heroBase" },
				{
					"required": ["variant"],
					"properties": { "variant": { "const": "basic" } }
				}
			]
		},

		"carouselHero": {
			"title": "Carousel Hero",
			"type": "object",
			"required": ["variant", "pages"],
			"properties": {
				"variant": { "const": "carousel" },
				"pages": { "type": "array", "items": { "$ref": "#/$defs/heroBase" } }
			}
		}
	}
}
