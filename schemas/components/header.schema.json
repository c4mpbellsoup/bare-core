{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"$id": "https://raw.githubusercontent.com/c4mpbellsoup/bare-schema/refs/heads/master/schemas/components/header.schema.json",
	"title": "Header",
	"type": "object",

	"properties": {
		"left": {
			"description": "The left section of the header.",
			"$ref": "#/$defs/headerSection"
		},
		"centre": {
			"description": "The centre section of the header.",
			"$ref": "#/$defs/headerSection"
		},
		"right": {
			"description": "The right section of the header.",
			"$ref": "#/$defs/headerSection"
		},
		"visible": {
			"description": "Whether to display the header.",
			"type": "boolean",
			"default": true
		}
	},

	"anyOf": [
		{ "required": ["left"] },
		{ "required": ["centre"] },
		{ "required": ["right"] }
	],

	"additionalProperties": false,

	"$defs": {
		"headerSection": {
			"type": "object",
			"required": ["type"],
			"unevaluatedProperties": false,
			"oneOf": [
				{
					"properties": {
						"type": {
							"const": "logo"
						}
					}
				},
				{
					"required": ["menu"],
					"properties": {
						"type": {
							"const": "menu"
						},
						"menu": {
							"$ref": "./menu.schema.json"
						}
					}
				}
			]
		}
	}
}
