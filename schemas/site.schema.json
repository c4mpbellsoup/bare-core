{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"$id": "https://raw.githubusercontent.com/c4mpbellsoup/bare-schema/refs/heads/master/schemas/site.schema.json",
	"title": "Bare Site",
	"description": "The schema for a Bare Site.",
	"type": "object",

	"required": ["schemaVersion", "name", "template", "theme"],

	"properties": {
		"schemaVersion": {
			"description": "The version of the Bare Site schema used by this site.",
			"type": "integer",
			"const": 1
		},
		"name": {
			"description": "The name of the site (business, organisation, etcetera).",
			"type": "string",
			"minLength": 1,
			"maxLength": 50
		},
		"description": {
			"description": "A short description of the site.",
			"type": "string",
			"maxLength": 150
		},
		"template": {
			"$ref": "./template.schema.json"
		},
		"theme": {
			"$ref": "./theme.schema.json"
		}
	}
}
