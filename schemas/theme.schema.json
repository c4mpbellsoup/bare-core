{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"$id": "https://raw.githubusercontent.com/c4mpbellsoup/bare-schema/refs/heads/master/schemas/theme.schema.json",
	"title": "Bare Theme",
	"description": "The schema for a Bare Site theme.",
	"type": "object",

	"properties": {
		"schemaVersion": {
			"description": "The version of the Bare Site theme schema used by this theme.",
			"type": "integer",
			"const": 1
		},
		"colours": { "$ref": "#/$defs/colours" },
		"typography": { "$ref": "#/$defs/typography" },
		"spacing": { "$ref": "#/$defs/spacing" },
		"shape": { "$ref": "#/$defs/shape" }
	},

	"required": ["schemaVersion", "colours", "typography", "spacing", "shape"],
	"additionalProperties": false,

	"$defs": {
		"colours": {
			"type": "object",
			"required": [
				"primary",
				"primaryContent",
				"neutral",
				"neutralContent",
				"base1",
				"base2",
				"base3",
				"baseContent",
				"info",
				"infoContent",
				"success",
				"successContent",
				"warning",
				"warningContent",
				"error",
				"errorContent"
			],
			"properties": {
				"primary": {
					"description": "The main colour of your brand.",
					"type": "string"
				},
				"primaryContent": {
					"description": "Foreground colour to use with the primary colour.",
					"type": "string"
				},
				"secondary": {
					"description": "The optional, secondary colour of your brand.",
					"type": "string"
				},
				"secondaryContent": {
					"description": "Foreground colour to use with the secondary colour.",
					"type": "string"
				},
				"accent": {
					"description": "The optional, accent colour of your brand.",
					"type": "string"
				},
				"accentContent": {
					"description": "Foreground colour to use with the accent color.",
					"type": "string"
				},
				"neutral": {
					"description": "A neutral dark colour.",
					"type": "string"
				},
				"neutralContent": {
					"description": "Foreground colour to use with the neutral color.",
					"type": "string"
				},
				"base1": { "description": "Base surface colour.", "type": "string" },
				"base2": { "description": "Darker base colour.", "type": "string" },
				"base3": { "description": "Darkest base colour.", "type": "string" },
				"baseContent": {
					"description": "Foreground colour to use with the base colours.",
					"type": "string"
				},
				"info": { "description": "Info colour.", "type": "string" },
				"infoContent": {
					"description": "Foreground colour to use with the info colour.",
					"type": "string"
				},
				"success": { "description": "Success colour.", "type": "string" },
				"successContent": {
					"description": "Foreground colour to use with the success colour.",
					"type": "string"
				},
				"warning": { "description": "Warning colour.", "type": "string" },
				"warningContent": {
					"description": "Foreground colour to use with the warning colour.",
					"type": "string"
				},
				"error": { "description": "Error colour.", "type": "string" },
				"errorContent": {
					"description": "Foreground colour to use with the error colour.",
					"type": "string"
				}
			}
		},
		"typography": {
			"type": "object",
			"required": ["heading", "body"],
			"properties": {
				"heading": {
					"description": "The primary font used for headings.",
					"type": "string"
				},
				"body": {
					"description": "The primary font used for body text.",
					"type": "string"
				}
			}
		},
		"spacing": {
			"description": "The overall spacing density of the site.",
			"type": "string",
			"enum": ["tight", "comfortable", "spacious"]
		},
		"shape": {
			"description": "The overall shape language of the site.",
			"type": "string",
			"enum": ["sharp", "soft", "rounded"]
		}
	}
}
